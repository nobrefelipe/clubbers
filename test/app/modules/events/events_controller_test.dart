import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:clubbers/app/modules/events/events_controller.dart';
import 'package:clubbers/app/modules/events/events_module.dart';

void main() {
  initModule(EventsModule());
  // EventsController events;
  //
  setUp(() {
    //     events = EventsModule.to.get<EventsController>();
  });

  group('EventsController Test', () {
    //   test("First Test", () {
    //     expect(events, isInstanceOf<EventsController>());
    //   });

    //   test("Set Value", () {
    //     expect(events.value, equals(0));
    //     events.increment();
    //     expect(events.value, equals(1));
    //   });
  });
}
