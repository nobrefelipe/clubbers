import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:clubbers/app/modules/events/events_page.dart';

void main() {
  testWidgets('EventsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EventsPage(title: 'Events')));
    //  final titleFinder = find.text('Events');
    //  expect(titleFinder, findsOneWidget);
  });
}
